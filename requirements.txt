# Aras-GP — one install covers the relay engine and the panel.
# Python 3.10+ required.
#
#   pip install -r requirements.txt
#
# The panel used to be missing from this file, so `run.sh` / `run.bat` built a
# virtualenv without Flask in it and `python -m panel` died on an import error
# that named a module and nothing else. Splitting the two lists only ever
# produced that failure, so they are one list.

# ── Panel ─────────────────────────────────────────────────────────────
Flask>=3.0.0
requests>=2.31.0

# ── Relay engine ──────────────────────────────────────────────────────
# MITM interception: mints the per-host certificates. Not optional.
cryptography>=41.0.0

# HTTP/2 multiplexing (faster Apps Script relay)
h2>=4.1.0

# CA bundle for TLS verification — skipped on Windows (uses system cert store).
certifi>=2024.1.0; sys_platform != "win32"

# Brotli decompression (modern websites send `br` encoding)
brotli>=1.1.0

# Zstandard decompression (some CDNs now serve `zstd`)
zstandard>=0.22.0

# LAN interface detection uses only the Python standard library
# (works on Windows, Linux, macOS, Android/Termux without a C compiler).
